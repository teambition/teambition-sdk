machine:
  node:
    version: 4.4.7

dependencies:
  override:
    - npm install
    - npm run typings
    - npm link
    - npm link teambition-sdk

test:
  pre:
    - npm run build_all
  post:
    - cp coverage/coverage.json $CIRCLE_ARTIFACTS/json_output

notify:
  webhooks:
    - url: http://teambition.vvlyn.com/api/circle